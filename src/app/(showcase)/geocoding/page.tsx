import { GeocodeComponent } from "./geocodeComponent";

export default async function GeocodingPlayground() {
    return (
        <div>
            Geocoding
            <GeocodeComponent />
        </div>
    );
}
